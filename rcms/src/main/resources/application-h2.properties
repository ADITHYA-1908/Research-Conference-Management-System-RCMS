# ============================================================
# RCMS Explicit H2 Development Profile (spring.profiles.active=h2)
# Use when running standalone without Docker or local MySQL.
# ============================================================
spring.datasource.url=jdbc:h2:mem:rcms_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=false
